#import Tree.py
import secrets
"""
#IDEA:
Tree stored on server
Client stores position of node on tree + max length of path
Client generates an access path and operations along that path
Client sends that path + operations to server
Server recieves access path + operations, performs operations along that path

"""

